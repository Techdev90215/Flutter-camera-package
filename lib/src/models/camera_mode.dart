// lib/src/models/camera_mode.dart

/// Defines the operational modes for the camera controller.
enum CameraMode {
  /// The camera is configured for taking single pictures.
  photo,

  /// The camera is configured for recording videos.
  video,

  /// The camera is configured for real-time barcode scanning.
  barcode,
}